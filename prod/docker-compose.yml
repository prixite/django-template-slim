version: '3.4'

services:
  db:
    image: postgres
    env_file:
      - .env

  sync-web:
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - .env
    volumes:
      - static_volume:/opt/code/static
      - shm:/dev/shm
    depends_on:
      - db
    command: bash ./scripts/run-with-gunicorn.sh

  async-web:
    build:
      context: .
      dockerfile: ./Dockerfile
    env_file:
      - .env
    volumes:
      - static_volume:/opt/code/static
      - shm:/dev/shm
    depends_on:
      - db
    command: bash ./scripts/run-with-gunicorn.sh

  nginx:
    build: ./nginx
    restart: always
    ports:
      - 80:80
    volumes:
      - static_volume:/opt/code/static
      - shm:/dev/shm
    depends_on:
      - api

volumes:
  static_volume:
  shm:
